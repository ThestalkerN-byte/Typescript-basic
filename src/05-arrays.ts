(() => {
  const prices: number[] = [1, 2, 3]; // declaro un array de numeros
  const values: (number | string | boolean | Object)[] = [1, 23, true, false]; // decalro un array para varios tipos de datos.
    // no es recomendable usar Object porque casi todo en  js es  un objeto
})();
